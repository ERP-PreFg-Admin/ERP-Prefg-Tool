.claude/worktrees/agent-a5bc0500299d42c64
check_uniware_apis
check_uniware_apis/*

uniware_documents
uniware_documents/*

# Secrets / local config for the sku-export helper — never track.
uniware_sku_export/.env

# Real supplier documents. This root Invoices/ is separate from erp_project/Invoices/,
# which erp_project/.gitignore already covers.
/Invoices/